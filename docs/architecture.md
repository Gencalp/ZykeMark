# ZykeMark Architecture

## Overview
ZykeMark is structured as a .NET solution with a core library, a CLI app, and a test suite. This file will evolve to capture system boundaries and data flow decisions.

## Layers
- **Core**: Domain interfaces and brand tokens.
- **CLI**: Entry point for local execution and orchestration.
- **Tests**: Validation of core domain behavior.
